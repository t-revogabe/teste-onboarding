import type { ImageSourcePropType } from "react-native";

// Register your image assets here.
// Each key must match the `image` field in your schema's select options.
//
// Example:
//   import { MyImage } from "@/assets/images";
//   export const ONBOARDING_IMAGE_REGISTRY: Record<string, ImageSourcePropType> = {
//     "my-image": MyImage,
//   };

export const ONBOARDING_IMAGE_REGISTRY: Record<string, ImageSourcePropType> = {};
